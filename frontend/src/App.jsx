// TODO: Implement in Issue 30
// - React Router setup with routes: /, /login, /repos, /repos/:id, /query
// - AuthContext provider wrapping all routes
// - ProtectedRoute wrapper for authenticated-only pages
// - Layout with navigation header

export default function App() {
  return <div>RepoRAG AI - Frontend scaffold</div>;
}
