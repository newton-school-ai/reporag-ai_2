"""Retrieval and generation evaluation metrics.

Computes standard IR metrics (context recall, precision, MRR, NDCG) and
generation metrics (faithfulness, relevance, hallucination rate, citation coverage).
"""

# TODO: Implement in Issue 34
# - context_recall_at_k(retrieved, relevant, k) -> float
# - precision_at_k(retrieved, relevant, k) -> float
# - mean_reciprocal_rank(retrieved, relevant) -> float
# - ndcg_at_k(retrieved, relevant, k) -> float
# - faithfulness(answer, context) -> float (LLM-as-judge)
# - answer_relevance(query, answer) -> float (LLM-as-judge)
# - hallucination_rate(answer, context) -> float
# - citation_coverage(answer, citations) -> float
